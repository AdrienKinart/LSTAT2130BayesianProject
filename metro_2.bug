model
{
    y[1:10] ~ dmulti(pi[], n)
    kappa <- 1/pimu
    lambda <- 1/(piphi * pimu)
    pimu ~ dnorm(mean, tau)
    piphi ~ dunif(0.00000E+00, dis)
    conjprior <- pimu * piphi
    gam ~ dgamma(kappa, lambda)
    llik <- sum(y * log(gam))
    logpost <- llik + log(conjprior)
}
